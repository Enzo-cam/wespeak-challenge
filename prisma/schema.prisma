// Nuevo schema
// Trabajamos con postgresql ya que levantaré una BDD en Railway.app/


datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Counter {
  id          Int      @id @default(autoincrement())
  value       Int      @default(0)
  lastUpdated DateTime @default(now()) @updatedAt
}